<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.basetech.testshow.dao.TestBaseDAO">
            
         <delete id="deleteTestBase" parameterType="TestBase">
             <!--  delete from test_base where (product_name=#{product_name}# and product_version=#{product_version}#) -->
              delete from test_base where (product_name=#{0}# and product_version=#{1}#)
         </delete>
          
</mapper>